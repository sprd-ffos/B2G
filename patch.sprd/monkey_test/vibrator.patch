diff --git a/vibrator/vibrator.c b/vibrator/vibrator.c
index f946ce1..4eae27c 100644
--- a/vibrator/vibrator.c
+++ b/vibrator/vibrator.c
@@ -42,6 +42,7 @@ int vibrator_exists()
 
 static int sendit(int timeout_ms)
 {
+#if 0
     int nwr, ret, fd;
     char value[20];
 
@@ -61,6 +62,8 @@ static int sendit(int timeout_ms)
     close(fd);
 
     return (ret == nwr) ? 0 : -1;
+#endif
+    return 0;
 }
 
 int vibrator_on(int timeout_ms)
